version: 2
jobs:
  build:
    docker:
      image: cimg/node:18-alpine
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test